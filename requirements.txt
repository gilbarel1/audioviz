# Python dependencies
numpy>=1.24.0
scipy>=1.10.0

# Audio libraries (install at least one)
librosa>=0.10.0  # Recommended primary backend
soundfile>=0.12.0  # Alternative backend
# pydub>=0.25.0  # Alternative backend (requires ffmpeg)

# IPC (POSIX systems only - Linux, macOS)
posix-ipc>=1.1.0; sys_platform != 'win32'

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
